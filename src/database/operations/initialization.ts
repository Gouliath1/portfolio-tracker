// Simple helper to ensure database is ready (no initialization logic)
// Database initialization happens at server startup via instrumentation.ts
export async function ensureDatabaseReady(): Promise<void> {
    // Database should already be initialized at server startup
    // This is just a placeholder for any future database readiness checks
}
